# ─────────────────────────────────────────────────────────────
# SafeScan — CORE dependencies (pinned)
# Runs: the Flask web app, the SVM trainer, and the fairness audit.
# These are the exact versions this project was verified against.
#   pip install -r requirements.txt
#
# For the deep-learning / multimodal features (Bi-LSTM, BERT, CLIP memes,
# Grad-CAM, SHAP-heavy paths) additionally install requirements-ml.txt.
# ─────────────────────────────────────────────────────────────

# Data & classical ML
numpy==2.0.2
pandas==2.3.3
scikit-learn==1.6.1
scipy==1.13.1
joblib==1.5.3
nltk==3.9.2

# Web app
Flask==3.1.3
flask-cors==6.0.5
Werkzeug==3.1.8
Jinja2==3.1.6
python-dotenv==1.2.1
